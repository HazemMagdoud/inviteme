# assets/templates/ — template asset folders (placeholders)

Category subfolders (birthday, floral, graduation, kids, luxury, minimal, party, wedding),
currently `.gitkeep` only. Templates render from config + user photos, not static files, so
these are optional. Use them if you later add curated hero/preview images per category.
